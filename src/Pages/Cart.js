import React from "react";
import styled from "styled-components";

const Cart = () => {
  return (
    <CartContainer>
      <h2>Cart page</h2>
    </CartContainer>
  );
};

export default Cart;

const CartContainer = styled.div`
  height: 100vh;
  margin-top: 120px;
`;
